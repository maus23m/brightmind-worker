# BrightMind Change Request Log

Last updated: 12 June 2026

| ID | Description | Severity | Status |
|---|---|---|---|
| CR-001 | Mobile nav bar truncation — top navigation buttons don't fit on mobile viewport. Fix: horizontal scroll with hidden scrollbar. | LOW | ✅ DONE (V2.13) |
| CR-002 | Show retake count on test results history — when child retakes same tutorial, display attempt number (e.g. "Attempt 2"). | LOW | ✅ DONE (V2.13) |
| CR-003 | Show test difficulty on test results history — display difficulty level (Easy/Medium/Hard) alongside topic name. | LOW | ✅ DONE (V2.13) |
| CR-004 | Number duplicate topic tutorials — when same topic has multiple tutorials, label them sequentially (e.g. "Standard Form #1", "#2"). | LOW | ✅ DONE (V2.13) |
| CR-005 | Children's Profiles page shows "undefined" next to avatar — avatar field null in database. Fix: fallback to 🧒 default. | LOW | ✅ DONE (V2.13) |
| CR-006 | No loading/progress indicators on button clicks — buttons give no visual feedback when actions are processing. Add spinner or loading state. | MEDIUM | ✅ DONE (V2.14, REOPENED & FIXED 1 Jun 2026) — original V2.14 work defined `.btn-loading` spinner CSS + `setLoading`/`clearLoading` utilities but **never wired them into any handler** (zero call sites), and the user-visible navigation gap (clicks on dashboard nav / wizard Next await Supabase round-trips with no feedback) was not addressed. Fix: top progress bar (`#nav-progress`) + global capture-phase click delegate that triggers `navStart()` on any nav-/async-triggering onclick (regex over handler names — `goTo*`, `wizNext`, `handleParentSignIn`, `confirmSave`, `saveChild`, `regenerate`, `replaceFlagged`, `recheckJob`, `startTest`, etc.). Cleared by `showScreen`/`showWizStep` on screen transition, by `showErr` on validation failure, by `showToast(_, 'error'\|'warn')` on action-failure toasts, and by an 8s failsafe. `body.nav-busy` also sets `cursor: progress`. Button-level `setLoading` utilities retained for handlers that already use textContent swaps. |
| CR-007 | Tutorial generation progress lost when parent navigates away — no way to resume or see result. Need to persist generation state and show pending tutorials on return. | MEDIUM | ✅ DONE (V2.14) — job persisted to localStorage; dashboard banner shows generating/ready/failed state; banner re-entry resumes polling or shows completed questions; job retained until tutorial saved. |
| CR-008 | Add child selection to tutorial setup page (Step 1) — parent should associate tutorial with a child upfront on the subject/year selection screen. | MEDIUM | ✅ DONE (V2.14) — child selector added to Step 1, auto-populated on year selection; validation requires child before proceeding. |
| CR-009 | Show tutorial completion count per topic on topic selection page — if child already has tutorials for a topic, display progress (e.g. "2/3 completed") next to topic name. | LOW | ✅ DONE (V2.14) — per-topic progress badge on topic chips (green = all done, amber = partial, grey = none done). |
| CR-010 | Question ratio selection for multi-topic tutorials — when parent selects multiple topics, allow them to set question distribution (e.g. 60:40, 50:50). | LOW | OPEN |
| CR-011 | Remove "Oxford × Cambridge Educational Initiative" text from login page. Replaced with "AI-Powered Maths & Science Tutoring". | LOW | ✅ DONE (V2.13) |
| CR-012 | "Children's Progress" and "Quick Actions" sections on dashboard appear empty on mobile — review and remove if no meaningful content rendered. Wasted vertical space. | LOW | OPEN |
| CR-013 | Topic Mastery tiles — tap should navigate to test results filtered by that topic. | LOW | ✅ DONE (V2.14) — single-tap on a mastery tile filters Test Results History to that topic with a clearable filter banner. Single-tap used instead of double-tap (better mobile pattern). |
| CR-014 | Streak Calendar — tap on a date should navigate to test results taken on that date. Improve calendar visual design. | LOW | ✅ DONE (V2.14) — active calendar days show day number and are tappable to filter results by date; inactive days non-clickable. |
| CR-015 | Extend the deterministic compute engine: add a `divide` op (enables verification of speed, density and other division-based calculable science currently routed to `verify:"none"`), and build the `equation_balance` verifier — an atom-conservation check for chemistry equation-balancing questions. `verify:"equation_balance"` is already a recognised generator tag; until this CR lands those questions pass through to the audit agent unverified. | MEDIUM | ✅ DONE (28 May 2026) — `compute.js`: added `divide` op AND went beyond scope for full GCSE/Year-11 coverage. New safe expression evaluator (hand-written tokenizer + shunting-yard, no eval/Function) powers a `formula` op (Pythagoras, area, compound interest, degrees trig, any multi-step expression) and a `solve_for` op (algebra by substitution, fail-closed on tie/no-root). `equation_balance` now verified by atom conservation (`parseFormula`/`parseEquation`/`balances`) in two sub-modes (coefficient placeholder, or which-equation-balances); bare tag with no compute block still passes through to audit. Match tolerance made relative (`matchTolFor`, 0.1% with a second-match ambiguity guard) for irrational answers. `prompts/question_gen.txt` updated with the new compute shapes, grammar, degrees note and worked examples. Tester: `compute.test.js` +42 cases (evaluator, formula, divide, solve_for, equation_balance), all 70 green; legacy single-op path unchanged. |
| CR-016 | Merge the Audit agent and Child agent into a single model call with a combined prompt. Both currently review the same question list in separate Claude calls; merging halves the model calls in stages 4–5 and reduces latency against the ~26s wall-clock budget. No correctness change — isolate from defect work, own Tester pass. | LOW | ✅ DONE (1 Jun 2026) — old stages 4 (`audit()`) and 5 (`childAgent()`) collapsed into one Stage 4 `review()` call. New `prompts/review.txt` runs both roles (curriculum auditor + Year-N student) in one combined prompt returning per-question `{audit, child}` verdicts; `prompts/audit.txt` + `prompts/child_agent.txt` deleted. Pure merge logic (`buildReviewQuestions`, `applyReview`) extracted to a standalone `review.js` module (like `compute.js`) — preserves the DEF-035 diagramPrompt awareness, the DEF-041 compute-verified `c` lock (`_auditCFixedToCompute`), audit rewrites (`_auditRewritten`), the `_auditFailed`/`_childFailed` flags, and fail-open behaviour. Handler keeps both CR-019 pipeline-rejection categories (audit vs child) distinct. One model round-trip saved (was 3000+1500 tokens sequential → one 4000-token call). Accepted nuance: the student now judges the original text even when the auditor rewrites (the prompt also tells the student to ignore `c`/`e`). Tester: new `review.test.js` (28 cases, all green) + `node compute.test.js` regression unchanged (70 green); `npm test` runs both. |
| CR-017 | One-off `question_bank` cleanup. Existing bank rows were written before the DEF-041 compute engine and never passed through verification; some may carry wrong answer keys. Bank questions served via `adaptBank` (Stage 0) bypass Stage 2 entirely. Migration script: recompute-verify every row whose answer is calculable, delete or correct rows where stored `c` does not match. Assumption to confirm: nothing writes to `question_bank` outside this pipeline — if true, a one-off migration is sufficient and no ongoing bank-path verification is needed; if false, a second CR for ongoing verification is required. | MAJOR | OPEN |
| CR-018 | Tighten `prompts/question_gen.txt` so `verify:"arithmetic"` questions choose input numbers that produce clean answers (whole numbers or sensible decimals). The compute engine currently rejects questions whose verified answer matches no option — a live example was a `mean` question resolving to 47.428571… Each rejection discards a generated question and forces a top-up call, wasting time against the 26s budget. Prompt-only change; pairs with an audit-validation review per the prompt-change rule. | LOW | OPEN |
| CR-019 | Feed rejection reasons back into top-up regeneration. The compute engine already writes a free-text `_computeReason` on every rejected question (e.g. "verified answer 40 not in options", "op closest_to: two candidates tied", "duplicate options"), but it is only logged to console — the top-up `generate()` call regenerates blind, so the model can repeat the same mistake. Change: collect `_computeReason` (and audit/child rejection reasons) from this job's rejected questions and inject them into the top-up prompt as a "questions rejected for these reasons — do not repeat" block. Lets Claude self-correct rather than rejection→top-up→same-error looping, and reduces wasted calls against the 26s budget. `index.js` top-up path only; pairs with a question_gen.txt note that a rejection-feedback block may be appended. Own Tester pass. | MEDIUM | ✅ DONE (26 May 2026) — `index.js`: pipeline rejections (compute/audit/child) collected with free-text `note` per question and passed into the top-up `generate()` call via the existing `rejections` param; `buildPrompt` now surfaces verbatim notes in a FEEDBACK block. Tester: covered by `compute_test.js` rejection-reason text + manual top-up log check. |
| CR-020 | Parent-side free-text rejection reason + feedback into replacement generation. On the "Set a Tutorial" review screen, when a parent discards a question (`discardQuestion`), the modal offers 7 preset reasons including "Other" but has NO free-text field — a parent choosing "Other", or wanting to add detail to a preset, cannot say what is actually wrong. Add an optional free-text textarea to the rejection modal (required when "Other" is picked, optional otherwise); store it in `question_rejections` alongside the preset `reason`. SECOND, and more important: `replaceFlagged` currently calls the Edge Function with only `count: n` — the rejection reasons are saved to the DB but never sent to regeneration, so the replacement question can repeat the rejected fault. Pass the rejected questions' text + preset reason + free-text note into the generate call so the generator avoids the same problem. This is the parent-facing twin of CR-019 (which feeds the compute engine's own pre-parent rejection reasons into top-up); both feedback loops are wanted. OWNERSHIP: Designer Agent — rejection modal UI + the regeneration prompt block; Orchestrator — `replaceFlagged` flow and Edge Function payload. Edge Function `generate()` gains an optional rejection-feedback parameter (shared with CR-019's mechanism if built together). Needs Tester coverage: modal requires text when "Other" selected; reason reaches the Edge Function payload; happy + unhappy (empty note, network failure) paths. | MEDIUM | ✅ DONE (26 May 2026) — discard modal now two-step: pick preset reason → optional free-text textarea (mandatory when "Other"); `submitRejection` validates and stores `note` in `question_rejections` (new `note` column, migration `add_note_to_question_rejections`). Edge Function `getRecentRejections` selects `note`; `buildPrompt` shows notes verbatim — same FEEDBACK block as CR-019. Tester: 5 new app-HTML cases (modal note field, note read, "Other" required, note stored). |
| CR-021 | New diagram evaluator agent (Stage 3.5). The Stage 3 drawing agent generates each SVG from a SELF-CONTAINED `diagramPrompt` and never sees the question, options or correct answer, so it cannot tell when a label is irrelevant (e.g. 4 squares labelled "equally spaced" when spacing is not the point) or leaks the answer (e.g. a "how many squares?" diagram with every square numbered). The `STRICT WHITELIST` rule in `diagram_system.txt` is enforced blind and is regularly violated. Add a vision-based reviewer between Stage 3 and Stage 4 that DOES see the rendered diagram + full question context and returns a corrected SVG with offending labels removed while preserving every necessary label; drop the question if unsalvageable (DEF-033 style). | MEDIUM | ✅ DONE (4 Jun 2026) — new Stage 3.5 `evaluateDiagrams()` in `index.js`: rasterizes each drawn SVG to PNG (`@resvg/resvg-js`, upscaled to 1200px wide so labels are legible) and sends the IMAGE + question/options/answer/diagramPrompt + editable SVG source to `DIAGRAM_MODEL` (Opus). `callClaude` extended to accept image content blocks. Pure shaping/parsing extracted to `diagram_review.js` (`buildDiagramReviewInput`, `parseDiagramReviewResult`) like `compute.js`/`review.js`. Verdicts: `corrected` → swap in cleaned SVG (`_diagramCorrected`); `unfixable` → drop question + push a `diagram review` rejection note into the CR-019 top-up feedback loop; any infra/parse error fails OPEN (keep original diagram, mirrors `review()`). New prompts `diagram_review_system.txt` (reuses the STRICT WHITELIST spirit; never drop necessary labels) + `diagram_review_user.txt`. Wired into both main and top-up paths; runs per-question evaluations concurrently (`Promise.all`) to bound latency against the ~26s budget. `Dockerfile` installs `fonts-dejavu-core fontconfig` (resvg renders blank text without a system font — the evaluator's whole job is reading labels). Tester: new `diagram_review.test.js` (18 cases — payload shaping + ok/corrected/unfixable/error parsing, markdown-fence tolerance), `npm test` runs all three suites green; render smoke test confirmed crisp label rendering. **HOTFIX (4 Jun 2026):** the first deploy produced ZERO questions (every job failed) — the top-level `require("@resvg/resvg-js")` ran at module load and the native binding failed to load in the Cloud Run image, so the worker module never loaded and every invocation errored. Fixes: (1) rasterizer now loaded LAZILY + guarded (`getResvg()`) so a missing/incompatible binding degrades Stage 3.5 to a no-op instead of crashing the worker; (2) Stage 3.5 made OPT-IN via `DIAGRAM_REVIEW_ENABLED` (default OFF) — pipeline behaves exactly as pre-CR-021 until explicitly enabled in the deploy env; (3) removed the committed `package-lock.json` to restore the repo's original `npm install` behaviour (the pinned lockfile was a suspected cause of the platform binary not installing). Verified: `index.js` loads cleanly with `@resvg` absent. To re-enable once the rasterizer is confirmed in the image: set `DIAGRAM_REVIEW_ENABLED=true`. **REVERTED (5 Jun 2026) — see CR-021a row for the full reasoning; both CRs removed together.** |
| CR-022 | Curriculum/topic coverage in a tutorial is neither guaranteed nor transparent. A broad topic (e.g. Year 7 "Expressions and Equations") is passed to the generator as a bare string, so the model freely clusters on a few sub-skills and silently drops others — a reported case had NO expanding-single/double-bracket questions — and nothing surfaces what was actually covered. Fix (both coverage + transparency, Claude-enumerated subtopics): `prompts/question_gen.txt` gains a CURRICULUM COVERAGE section instructing the generator to identify the topic's Year-N National-Curriculum sub-skills and spread the question set across them (naming expanding brackets explicitly as not-to-omit), and to emit a `"subtopic"` label on every question. `index.js` captures `subtopic` in `generate()`, carries it through the pipeline, returns it in `cleanQuestions` (so it reaches the frontend), banks it (`bankWrite`) and reads it back in `adaptBank` (bank-sourced questions stay transparent), and logs the per-job subtopic distribution. `review.js` preserves `subtopic` across audit rewrites (like `svg`). Frontend `index.html`: new `renderCoverageSummary()` renders a "Subtopics covered" chip strip on the Step-4 review screen (refreshed by `renderPreview`, counts non-flagged questions) plus a per-question subtopic label; persistence is automatic (`tutorials.questions` is jsonb). DB: additive nullable `question_bank.subtopic` column (migration `add_subtopic_to_question_bank`). No Edge Function change — `job-status` relays `generation_jobs.questions` verbatim. Partially advances DEF-036 (finer-grained tagging than topics[0]). Tester: `prompts.test.js` +5, `review.test.js` +2 (rewrite + passthrough subtopic preservation), frontend tester +5 (coverage container/function/label). `npm test` green (compute 70 + review 30 + prompts 27). | MEDIUM | ✅ DONE (5 Jun 2026) |
| ADMIN-1 | Admin backend — first slice: move the four runtime model/token dials out of env-only into a Supabase `runtime_config` table the worker reads live (no redeploy to retune), with a full change audit and an admin-gated screen to edit them. Foundation that proves the admin-app + RLS + audit pattern before the heavier curriculum-curation slice (which builds on the same shell). See `BrightMind_Admin_Backend_Spec.md`. **DONE (7 Jun 2026)** — **DB** (Supabase MCP migration `admin_runtime_config`, idempotent; mirrored in-repo at `migrations/0001_admin_runtime_config.sql`): new `runtime_config` (key/value jsonb/value_type/description/updated_by/updated_at), append-only `config_audit`, and `admin_users` (admin gate keyed by `auth.users` uid, separate from the child-facing `users` table). RLS on all three — admins only via a `SECURITY DEFINER public.is_admin()` helper; the worker uses the service role (bypasses RLS); no anon access (verified: anon SELECT returns 0 rows). An `AFTER INSERT/UPDATE` trigger (`log_runtime_config_change`, SECURITY DEFINER) writes every change to `config_audit` so the audit can't be skipped; its direct RPC EXECUTE is revoked (clients can't forge audit rows — the trigger still fires). Seeded the 4 keys at current code defaults. **Worker** (`index.js`): `getConfig(url,key,configKey,fallback,fetcher=supaFetch)` with a 60s per-instance TTL cache + pure `_parseConfigValue` (typed coercion); resolved per-job into a `cfg` object (concurrency-safe, never module state) threaded through `generate`/`processDiagrams`/`drawDiagram`/`review`. Any miss/error/unreachable table falls back to the env/hardcoded default, so behaviour is unchanged until a value is set. Job-start log now prints each resolved dial + whether it came from `config` or `default` (DEF-040 discipline). `CONFIG_DEFAULTS` is also the functions' default `cfg`. **Admin app** (`frontend/admin.html`, served at `/admin.html` by the existing Netlify publish dir — no second site): vanilla-JS SPA mirroring the parent app's lightweight Supabase client; Supabase Auth sign-in + `admin_users` gate; list/inline-edit `runtime_config` (writes set `updated_by`=email, trigger logs audit); read-only recent-changes panel. **Scope:** only the 4 model/token dials (all in `index.js` with env fallbacks); `EPS`/`MATCH_TOL` (in `compute.js`'s sync engine) and generation counts deferred to a later slice. Tester: new `config.test.js` (16 cases via an injected fake fetcher — value-present→used, absent→fallback, fetch-error→fallback, TTL cache hit + expiry refetch, type coercion); `npm test` green (compute 70 + review 30 + prompts 27 + config 16). Migration idempotency, audit old→new, and RLS-anon-blocked all verified via MCP. First admin must be bootstrapped manually (RLS blocks self-promotion — SQL in the migration footer). | MEDIUM | ✅ DONE (7 Jun 2026) |
| ADMIN-2 | Admin backend — Slice 2: curriculum curation & approval. Replace the generator's on-the-fly sub-skill guessing (which silently drops sub-strands) with human-approved curriculum knowledge the worker reads at generation time. The loop: offline sweep proposes a maximum-recall union taxonomy → human prunes/edits/approves in the admin app → frozen, versioned `curriculum_objects` row → worker steers generation from it (falls back to self-enumeration where no approved object exists). Builds on the Slice-1 admin shell + RLS + audit. See `BrightMind_Admin_Backend_Spec.md`. **DONE (7 Jun 2026)** — **DB** (MCP migration `curriculum_curation`, idempotent; mirror `migrations/0002_curriculum_curation.sql`): `curriculum_objects` (frozen, versioned, `payload` = sub_strands/prerequisites/misconceptions) + `curation_proposals` (max-recall union, provenance + year_flag) + RLS (admins only via reused `is_admin()`; worker reads via service role) + audit trigger → existing `config_audit` + atomic `approve_curation_proposal()` RPC (next-version/archive-prior/stamp/mark-approved, internal admin guard). Seeded one real proposal from the Y7 Expressions & Equations demo doc. **Shared module** `curriculum.js` (pure, tested): `validateProposalPayload` / `parseSweepResult` / `latestApprovedPerTopic` / `buildCurriculumGuidance`. **Worker** (`index.js`): `getApprovedCurriculum()` (service-role read, errors/empty → fallback) resolved per job and threaded `generate`→`buildPrompt`; new `{{curriculum}}` block in `prompts/question_gen.txt` (approved = authoritative, self-enumeration = explicit fallback — prompt+validation reviewed together, no schema change since `subtopic` still required per question); job log prints steered/fallback topic split. **Offline sweep** `scripts/curriculum_sweep.js` + `prompts/curriculum_sweep.txt`: operator-run (`--subject/--topic/--year/--source/--dry-run`), best-effort source fetch (records reachability; degrades to Claude's knowledge), distils max-recall union → validates → writes a `pending_review` proposal. Never the worker/hot path. **Admin app** (`frontend/admin.html`): Config | Curriculum tabs; proposal review with per-sub-strand prune (untick), inline edit, add, provenance/year_flag badges; Approve (RPC) / Reject; read-only approved-objects list. **Scope:** steer-with-fallback (no hard gating). Tester: new `curriculum.test.js` (20) + `prompts.test.js` (+6) — `npm test` green (compute 70 + review 30 + prompts 33 + config 16 + curriculum 20). Verified via MCP: migration idempotent, approval RPC (version/archive/audit/guard), RLS blocks anon, advisor clean (only the intentional admin-guarded RPC flagged). Left a pristine first-run state (1 pending proposal, 0 approved). Live sweep needs the operator's ANTHROPIC_API_KEY (not run here). **Batch sweep (7 Jun 2026):** extended `scripts/curriculum_sweep.js` with a per-subject+year batch mode (`--subject maths --year 7 [--yes] [--topics "A,B"] [--limit N]`) that enumerates topics from a new canonical `curriculum_taxonomy.json` (mirrors the frontend `CURRICULUM_KS*_*` constants), skips topics already pending/approved, and guards cost with a plan/confirm step (prints the plan; no Claude calls or writes without `--yes`). Single-topic mode unchanged. Pure `topicsFor`/`planTargets` factored + exported; new `sweep.test.js` (20). Verified offline: `--subject maths --year 7` enumerates 24 topics; `--topics`/`--limit`/unknown-topic warning all correct. `npm test` green (compute 70 + review 30 + prompts 33 + config 16 + curriculum 20 + sweep 20). Taxonomy now lives in two places → DEF-048 (sync debt). **In-app Run sweep (7 Jun 2026):** deployed an admin-gated Supabase Edge Function `run-sweep` (`supabase/functions/run-sweep/index.ts`; verifies caller ∈ `admin_users`, distils one topic via Claude, writes a `pending_review` proposal, skips existing) and added a "Run sweep" panel to the admin Curriculum tab (subject + year + topic, or all-for-year with a client-side loop + progress). Moved `curriculum_taxonomy.json` → `frontend/` so the app can fetch it (one canonical json; CLI + test paths updated). **Operator step:** none — `ANTHROPIC_API_KEY` already exists as a project-wide Supabase secret (shared with every Edge Function), so `run-sweep` reads it automatically. The sweep prompt + payload validator are re-ported into the Deno function (DEF-048 covers the sync debt). **Confirmed working in-browser (7 Jun 2026)**: admin → Curriculum → Run sweep → proposal created. (The build sandbox can't reach supabase.co directly, so it was verified live from the admin app rather than curl.) | MEDIUM | ✅ DONE (7 Jun 2026) |
| CR-021a | Make the Stage 3.5 rasterizer reliable. The hotfix left the feature safe but unusable — the native `@resvg/resvg-js` binding still wouldn't load in the Cloud Run image (npm optional-dependency / glibc footgun). Switch to a rasterizer that loads identically everywhere so the feature actually works when enabled. | MEDIUM | ✅ DONE (4 Jun 2026) — swapped `@resvg/resvg-js` (native) for `@resvg/resvg-wasm` (pure WASM, no per-platform binary). `index.js` `ensureRasterizer()` lazily `initWasm()`s the bundled `index_bg.wasm` once (cached promise shared by concurrent renders) and supplies fonts as buffers vendored under `assets/` (`DejaVuSans.ttf` + `-Bold`), since WASM has no system fonts; `renderSvgToPng` is now async (`await`ed in `evaluateDiagram`). Lazy + guarded + opt-in (`DIAGRAM_REVIEW_ENABLED`, still default OFF) all preserved — a load failure still degrades Stage 3.5 to a no-op. `Dockerfile`: dropped the `apt-get fonts-dejavu-core fontconfig` layer (fonts vendored) and added `RUN node scripts/render_check.js`, a build-time check that fails the build if the rasterizer can't turn text into a non-blank PNG — so a broken rasterizer never deploys (loud pre-deploy failure instead of silent prod no-op). `package-lock.json` gitignored (repo's no-lockfile convention). Verified: render_check passes (text PNG differs from blank), `npm test` green (70+28+18), worker loads with `assets/` missing (no crash), and a WASM-rendered sample shows crisp regular + bold labels. Enable in prod with `DIAGRAM_REVIEW_ENABLED=true` after confirming no `rasterizer unavailable` log line. **REVERTED (5 Jun 2026) — CR-021 + CR-021a pulled in full (Stage 3.5 removed). Two reasons: (1) IT NEVER WORKED in prod — every diagram logged `[DiagramReview] Error (keeping original): Claude 400 ... image.source.base64: invalid base64 data` (the rasterized PNG reached the API as malformed base64). It failed OPEN, so it corrected nothing — just added a failing Opus call + latency per diagram. (2) WRONG DESIGN — it existed to compensate for Stage 3 being deliberately blind to the question/answer; that is symptom-patching engineered blindness. The real fix is the draw prompt itself (see DEF-045), not a second vision agent. Removed: `diagram_review.js`, `diagram_review.test.js`, `scripts/render_check.js`, `prompts/diagram_review_{system,user}.txt`, vendored `assets/DejaVuSans*.ttf`, the `@resvg/resvg-wasm` dependency, the Dockerfile `render_check` line, the `evaluateDiagram(s)`/`ensureRasterizer`/`renderSvgToPng` wiring + `DIAGRAM_REVIEW_ENABLED` reads in `index.js` (both main and top-up paths), and the `diagram_review.test.js` entry in the test script. The image content-block path in `callClaude` was used only by Stage 3.5; the generic multi-turn message-array support is kept (harmless, provider-general) with the image/CR-021 wording dropped. `DIAGRAM_REVIEW_ENABLED` env var may stay set to false in Cloud Run harmlessly — the code no longer reads it. CR-016 (audit+child merge) left untouched. `node --check index.js` clean; `npm test` green (compute 70 + review 28 + new prompts 15). Do NOT re-attempt this design — fix bad diagrams at the draw prompt (DEF-045).** |
| CR-022 (cont.) | **Reopened 9 Jun 2026 — finish CR-022 (see DEF-049).** The 5 Jun "DONE" shipped only the 1D width slice; the spec's core — **mastery = coverage × performance × depth**, a per-child 2D `sub_strand × depth` grid that both reports coverage and steers generation — was unbuilt. Now that the ADMIN-2 curriculum-curation backend supplies authoritative `sub_strands` + `depth_bands`, build the full model in 5 safe sub-slices (A tagging → B matrix → C recommendation → D width-adaptive driver → E gap-aware top-up). **AXIS DECISION (locked):** depth (recall→procedure→application→reasoning) is **orthogonal** to the existing Easy/Medium/Hard difficulty — the matrix is `sub_strand × depth`; difficulty is within-cell granularity (a hard-recall and an easy-reasoning question both exist). Slice D's coverage driver steers *which cell*; difficulty adaptivity sets *how hard within it* — they compose, the driver never overrides difficulty. **DEPTH VALIDATION (amendment):** the generator must not free-label depth — the prompt anchors it with depth-labelled exemplar cells from `Coverage_Taxonomy_Demo_Y7_Expressions_Equations.md` (few-shot, CR-027 direction); a depth-tag agreement number is measured and the parent *mastery* recommendation is gated on it (below threshold, the UI shows *observed* coverage, not a judgement). **Fallback sacred:** no approved object → byte-for-byte today's behaviour; invalid/absent depth or unmatched subStrand never drops a question; NULL depth = unknown, never "all bands covered". Owner: Curriculum + Designer + Orchestrator. **CODE COMPLETE (9 Jun 2026), gated** — **Slice A (2D tag):** `prompts/question_gen.txt` now requires `subStrand` (width, copied verbatim from the approved block when present) + `depth` (one of 4 bands, anchored to depth-labelled exemplar cells, with a "depth is NOT difficulty / spread across both axes" rule) on every question; `subtopic` kept as a one-release alias. `index.js generate()` parses/normalises both, aligns `subStrand` to the canonical approved name via new pure `curriculum.js approvedSubStrandIndex` (off-list → kept + warned, never dropped; invalid depth → null, never dropped). `review.js applyReview` carries `subStrand`+`depth` across audit rewrites (same class as `subtopic`/`svg`). `bankWrite`/`adaptBank`/`cleanQuestions` write/read/surface the tags; per-job log now prints the 2D (sub-strand × depth) spread. Migration `coverage_2d` (mirror `migrations/0003_coverage_2d.sql`): `question_bank.sub_strand`+`depth` (idempotent, backfilled from `subtopic`), coverage index, seeded `COVERAGE_WEAK_PCT` dial. **Slice B (matrix):** new pure `coverage.js` — `buildCoverageMatrix` (projection over `results.answers`, NO new table), `gridStatus` (untested/weak/strong; NULL depth lights no cell → DEF-049 invariant), `untestedCells`. **Slice C (recommendation):** `coverage.js recommendation` + frontend Coverage Map card (`renderCoverageMastery`) + 2D `renderCoverageSummary` (width chips + depth strip); answer records (MCQ + freeform) now carry the tags so the matrix is derivable. **Gated per amendment 2:** the card shows OBSERVED coverage ("practised"), NOT a mastery verdict, until the depth-tag agreement number clears threshold. **Slice D (driver):** `coverage.js buildCoverageTargetGuidance` + new `{{coverage_target}}` prompt block (soft steer, composes with difficulty); handler reads the child's matrix (`getChildResults`) and threads it into both generate calls. **Slice E (top-up):** `adaptBank` gains a gap-cell bank tier before difficulty (width gap-fill now; depth-gap fill grows as the bank re-tags). **Fallback preserved** end-to-end: no approved object / no child / no tagged history → byte-for-byte today's behaviour. **AXIS = orthogonal** (depth × difficulty), written into the spec. Tester: new `coverage.test.js` (36, incl. the two spec-mandated tests + agreementStats) + `curriculum.test.js` (+10) + `prompts.test.js` (+9) + `review.test.js` (+2 of the +4 cases) + frontend self-tests (+6); `npm test` green (compute 70 + review 33 + prompts 42 + config 16 + curriculum 30 + sweep 20 + coverage 36). `node --check` clean (incl. HTML script). **STILL OPEN before final close (DEF-049):** run `scripts/depth_tag_check.js` (operator key) to record the depth-tag agreement number, then promote the recommendation from observed→confident if it clears threshold. | MEDIUM | CODE COMPLETE — gated on depth-tag agreement number (9 Jun 2026) |
| CR-031 | **Allow re-runs in curriculum sweeps.** Requested 12 Jun 2026: the sweep (in-app and CLI) skips any topic that already has a pending_review proposal or an approved object, so a topic can never be re-swept (e.g. to refresh a proposal with a newer model or after pruning regrets). SEMANTICS: a re-run supersedes — the existing `pending_review` proposal for the exact (subject, topic, year, scheme) is set `status='superseded'` (plain text column, no migration needed; audit trigger logs the change) and a fresh proposal is written; an APPROVED object is never touched by the sweep — it stays live until the fresh proposal is approved, at which point the existing `approve_curation_proposal` RPC does next-version + archive-prior as designed. The review queue therefore never holds two live proposals for one target. BUILT (12 Jun 2026) across all three sweep surfaces (the DEF-048 sync trio, moved together): (1) **Edge Function** `supabase/functions/run-sweep/index.ts` — accepts `force` in the body; skip bypassed when forced; pending proposals PATCHed to superseded before the fresh write. (2) **Admin app** `frontend/admin.html` — "Re-sweep existing" checkbox (`sw-force`) in the Run sweep panel, sent as `force`; description text explains the supersede semantics. (3) **CLI** `scripts/curriculum_sweep.js` — `--force` flag; `planTargets({force})` disables the skip (still respects `--topics`/`--limit`); new `supersedePending()` called before each write in both single and batch modes. Owner: Curriculum (sweep logic) + Orchestrator (screen flow). Tester: `sweep.test.js` +10 (force planTargets x3, admin wiring x2, Edge Function source x3, CLI x2); `npm test` green (compute 70 + review 33 + prompts 57 + config 16 + curriculum 30 + sweep 37 + coverage 36 = 279); `node --check` clean (CLI + admin inline script). **DEPLOY NOTE:** the Edge Function redeploy to `rtyvomkhajyinlycgjzm` is pending (MCP deploy permission denied in-session); until deployed, the checkbox sends `force` but the live function ignores it (safe no-op — old behaviour). | MEDIUM | CODE COMPLETE (12 Jun 2026) — pending Edge Function redeploy |
