# BrightMind Defect Log

Last updated: 14 June 2026

## Resolved

| ID | Description | Severity | Status |
|---|---|---|---|
| DEF-001 | Sign In button does not advance to dashboard | CRITICAL | ✅ RESOLVED |
| DEF-002 | Topic checkboxes do not respond to click | CRITICAL | ✅ RESOLVED |
| DEF-003 | All data lost on page refresh | CRITICAL | ✅ RESOLVED |
| DEF-004 | Question generation hangs for 30+ seconds | CRITICAL | ✅ RESOLVED |
| DEF-005 | Cannot navigate beyond first 3 questions | CRITICAL | ✅ RESOLVED |
| DEF-006 | Saved tutorials not visible to parent | MAJOR | ✅ RESOLVED |
| DEF-007 | Timer shows numeric countdown, not a bar | MAJOR | ✅ RESOLVED |
| DEF-008 | Child selector hardcoded, not reading from stored profiles | MAJOR | ✅ RESOLVED |
| DEF-009 | App opening directly signed in without user knowing | MAJOR | ✅ RESOLVED |
| DEF-010 | Call stack crash on child login | CRITICAL | ✅ RESOLVED |
| DEF-011 | Dashboard stale cache after changes | MAJOR | ✅ RESOLVED |
| DEF-012 | Results not saving — RLS blocking | CRITICAL | ✅ RESOLVED |
| DEF-013 | Streak/badges not updating | MAJOR | ✅ RESOLVED |
| DEF-014 | Save allowed with flagged questions pending | MAJOR | ✅ RESOLVED |
| DEF-015 | Edge Function 500 after successful Claude call — prompt/validation mismatch | CRITICAL | ✅ RESOLVED |
| DEF-016 | Progress charts blank on navigation — canvas rendering timing | MAJOR | ✅ RESOLVED |
| DEF-017 | JWT token expiry causing silent write failures | CRITICAL | ✅ RESOLVED |
| DEF-018 | Bad diagram quality — JSXGraph replaced with direct Claude SVG | MAJOR | ✅ RESOLVED |
| DEF-019 | Question count less than requested — top-up Claude call added | MAJOR | ✅ RESOLVED |
| DEF-020 | Science questions used character framing — phenomenon-first rule enforced | MEDIUM | ✅ RESOLVED |
| DEF-021 | Floating battery label in electricity diagram — proper circuit SVG example added | MEDIUM | ✅ RESOLVED |
| DEF-022 | Duplicate questions in same tutorial — text dedup + exclude list on top-up | MAJOR | ✅ RESOLVED |
| DEF-023 | Hardcoded topic lists causing bare calculations — principle-based prompt | MEDIUM | ✅ RESOLVED |
| DEF-024 | Semantic near-duplicates slipping through — Audit Agent set-level diversity check | MEDIUM | ✅ RESOLVED |
| DEF-025 | Starter question buttons not firing — JSON.stringify double quotes broke onclick | MAJOR | ✅ RESOLVED |
| DEF-026 | Answer leak in diagrams — labels revealed correct answer | CRITICAL | ✅ RESOLVED |
| DEF-027 | Diagram labels not following whitelist — extra labels added by drawing agent | MAJOR | ✅ RESOLVED |
| DEF-028 | Answer leak via colour coding — correct option drawn in green | MAJOR | ✅ RESOLVED |
| DEF-029 | Diagram showed all 4 options instead of the scenario | MAJOR | ✅ RESOLVED |
| DEF-030 | Cross-parent data leak — permissive RLS policies | CRITICAL | ✅ RESOLVED (V2 auth model) |
| DEF-031 | Wrong-child data leak within same family | CRITICAL | ✅ RESOLVED (V2 auth model) |
| DEF-032 | Diagram max_tokens was 4096 causing complex SVGs to fail silently — raised to 16000 | MAJOR | ✅ RESOLVED |
| DEF-039 | Diagram system prompt over-simplified. The full rule set in `diagram_system.txt` was stripped to a minimal version on the mistaken theory that the rules were degrading geometric accuracy. This was a misdiagnosis (see DEF-040). The simplification removed genuine safeguards (answer-leak whitelist enforcement, label-overlap rules). Fix: reverted `diagram_system.txt` to the full-rules version. Deployed 23 May 2026. | MAJOR | ✅ RESOLVED (DEPLOYED) |

## Open / In Progress

| ID | Description | Severity | Status | Owner |
|---|---|---|---|---|
| DEF-033 | Diagram-dependent questions served without diagram. When drawDiagram fails, question served with needsDiagram=true but no svg. Fix: drop question, log diagnostic, top-up fills gap. Fix is in `index.js` `processDiagrams` — on diagram-generation failure after 2 attempts the question is dropped (not pushed to results) and a DROPPED log line is emitted. | CRITICAL | RESOLVED (12 Jun 2026) — Tester: prompts.test.js (structural assertions: DROPPED log message present; results.push gated on svg truthy) | Orchestrator |
| DEF-034 | Top-up questions skip audit and child agent. Stages 4 and 5 bypassed. Unaudited questions reach children. FIX (12 Jun 2026): `index.js` top-up block now calls `review()` immediately after `processDiagrams`, filters `_auditFailed` + `_childFailed` from the result, and logs `Top-up Stage 4: N/M passed` — identical to the main path. Only `topupPassed` enters dedup and bankWrite. Note 25 May 2026: the DEF-041 compute engine routes top-up questions through Stage 2 verification correctly — DEF-034 (audit bypass) was the remaining gap. | MAJOR | RESOLVED (12 Jun 2026) — Tester: prompts.test.js (2 structural assertions: top-up calls review() after processDiagrams; topupPassed filters audit/child failures) | Orchestrator |
| DEF-035 | Audit agent has no diagram awareness. Receives hasDiagram boolean but never sees diagramPrompt. Cannot catch diagram-question mismatches. FIX: `review.js` `buildReviewQuestions()` carries `diagramPrompt: q.diagramPrompt || null` in the questions payload (DEF-035 comment in code). `review.txt` criterion 3 explicitly checks `diagramPrompt` for scenario/data mismatch (same numbers, units, context) and coordinate answer leaks — confirmed 12 Jun 2026 on re-audit of code vs. defect log. | MAJOR | RESOLVED (already in main — confirmed 12 Jun 2026) — Tester: review.test.js ("payload: diagramPrompt carried"); prompts.test.js (2 assertions: review.js carries diagramPrompt; review.txt criterion 3 checks it for mismatch) | Reviewer |
| DEF-036 | All banked questions tagged with first topic only. q._topic never set during generation. bankWrite defaults to topics[0]. | MINOR | OPEN [Note 5 Jun 2026 (CR-022): questions now also carry a finer-grained `subtopic` label (generator-assigned, banked in the new `question_bank.subtopic` column, surfaced to the frontend). This is partial progress on coverage tagging, but the core defect stands: `q._topic` is still never set, so the bank `topic` column still defaults to `topics[0]` for multi-topic jobs.] | Curriculum |
| DEF-037 | DIAGRAM_MAX_TOKENS env var not used. Code uses hardcoded 16000. | MINOR | RESOLVED (5 Jun 2026) — `index.js` now reads `MAX_TOKENS` (||8000) and `DIAGRAM_MAX_TOKENS` (||16000) as env-driven consts; `callClaude` defaults to `MAX_TOKENS` and the diagram stage passes `DIAGRAM_MAX_TOKENS` (the old hardcoded 16000 literal is gone). Fallbacks equal the previous values so an unset var changes nothing on deploy. Resolved caps are logged at job start (`maxTok=… diagMaxTok=…`) per the DEF-040 log-every-variable discipline. | Orchestrator |
| DEF-038 | Nutrition & Digestion diagram example is decorative — labels organs but contains no question-specific data. Child cannot derive answer from diagram alone. Needs rewriting with measurable data. Same pattern may affect Cells example. Re-audit (12 Jun 2026): the nutrition/digestion example was already data-rich ("churns food for 4 hours", "7 m long", "1.5 m long") and needed no change. The plant-cell example (lines 283-292) was the remaining decorative one — labels only, no counts. FIX: prose now specifies "exactly 5 small oval chloroplasts" and vacuole "fills about 60% of the cell area"; label whitelist updated to "Chloroplasts — '5 visible'" and "Vacuole — '60% of cell area'". Follows the atom example pattern (counts explicit in both prose and labels). | MEDIUM | RESOLVED (12 Jun 2026) — Tester: prompts.test.js (2 assertions: chloroplast count present, vacuole % present) | Designer |
| DEF-048 | Curriculum taxonomy is defined in two places that can silently diverge. The topic list lives both in the frontend (`frontend/index.html` `CURRICULUM_KS*_*` constants, the source parents pick from) and now in `curriculum_taxonomy.json` (read by the batch sweep). Topic chips are the integration key for `curriculum_objects`, so if someone edits topics in the app but not the JSON (or vice-versa), the sweep proposes against stale/mismatched topics and the worker's approved-object lookup silently misses (falls back). No automated guard catches drift (the frontend is a single HTML, not importable by the node tests). Mitigation for now: both copies created from the same strings this session; `sweep.test.js` guards the JSON's shape. Proper fix (deferred): make the frontend fetch `curriculum_taxonomy.json` so there is one source of truth, or add a CI check that diffs the two. **Also (7 Jun 2026):** the `run-sweep` Edge Function (`supabase/functions/run-sweep/index.ts`) re-ports the sweep prompt (`prompts/curriculum_sweep.txt`) and the payload validator (`curriculum.js`) into Deno — same sync-debt class; keep the three in step until a shared source is introduced. (The taxonomy json was moved to `frontend/` and is now fetched by both the CLI and the admin app, removing that one copy.) | MINOR | OPEN | Curriculum |
| DEF-047 | Supabase password change/reset not working for the admin account. The first admin (`maus23@gmail.com`) was bootstrapped with a temporary password (ADMIN-1); the Supabase password-reset link does not complete the change correctly, so the temp password remains in use. The admin app (`frontend/admin.html`) is sign-in only — no change-password screen. Low urgency: single internal operator, account is RLS-gated, power comes only from the `admin_users` row. Fix options: add a recovery-email reset flow to the admin app, set a new password via the Supabase dashboard (Authentication → Users), or debug the recovery redirect-URL config. Logged per "log the defect before fixing it"; fix deferred by owner. | MINOR | OPEN | Orchestrator |
| DEF-040 | Diagram quality gap caused by model mismatch, not prompt. The pipeline diagram stage ran on Sonnet 4 (`claude-sonnet-4-20250514`) while the test "Diagram Engine" ran on a stronger model (Opus). The two were never a controlled comparison — model was an unlogged variable, so repeated diagram defects were wrongly attributed to prompt quality. Fix: dedicated `DIAGRAM_MODEL` env var (default `claude-opus-4-7`) used only for the diagram stage; question generation, audit and child agent stay on the default model. Model string now logged on every diagram call and at job start so it can never again be an invisible variable. Deployed 23 May 2026 — Cloud Run logs confirm diagram stage running on `claude-opus-4-7`. Awaiting comprehensive visual quality check before final close. Lesson: when comparing pipeline output against a test harness, log the model on both sides. | MAJOR | DEPLOYED — awaiting visual QA | Designer |
| DEF-042 | Ambiguous select-among-the-options questions bypass the Compute Engine. Surfaced by a Year 7 number-line question "Which number is closest to 50?" options {46,48,52,55} marked 48 correct — but 48 and 52 are BOTH exactly 2 from 50: two correct answers, and the explanation ("48 is only 2 away… closer than the others") is false. ROOT CAUSE: questions whose answer is chosen by comparing the options (closest-to, largest, smallest, equal-to) are calculable but had no `op`, so the generator tagged them `verify:"none"` → audit agent → no arithmetic check (the DEF-041 class of gap). Separately, NOTHING checked for duplicate/ambiguous options on any question, including `verify:"none"` ones. CLASS of problem: any MCQ that can have more than one correct answer. FIX (26 May 2026 — broad): (1) four tie-rejecting comparison ops in `compute.js` — `closest_to`, `largest`, `smallest`, `equal_to`; each returns null (→ reject) when two options are equally correct. (2) UNIVERSAL duplicate-option guard in `computeVerify` runs on EVERY question regardless of verify type — rejects any MCQ with two numerically-equal or textually-identical options (catches non-numeric ambiguity on `verify:"none"` too). (3) answer-to-option matching rewritten to match the CLOSEST option, not the first within tolerance — fixes mis-match on closely-spaced decimals (0.6 vs 0.605); tie detection uses a tight epsilon (1e-9) separate from the answer-match tolerance (0.01). Generator updated (`prompts/question_gen.txt`): new ops + worked example + a general "no ambiguous questions / all four options distinct" rule. `index.js` unchanged — `computeVerify` already runs on the full generated array so the guard covers `verify:"none"`. TESTER: `compute_test.js` — 28 cases incl. each comparison op, each tie case, the exact DEF-042 question, numeric + text duplicate-option rejection, the decimal closest-match fix. RELATED: CR-019 logged to feed `_computeReason` free text back into top-up regeneration. | MAJOR | RESOLVED (26 May 2026) — Tester: compute_test.js |
| DEF-043 | Answer leak in coordinate-identification diagrams. Three Year 7 Graphs & Functions questions flagged "Answer leaked" by parent on 27 May 2026 — plotted points were labelled with their full coordinates (`B (3, -2)`, `P(3, 2)`) in the SVG, exposing the exact value the question asked the child to read from the axes. ROOT CAUSE: coordinate-identification questions are the one diagram type where "where to draw the point" and "what to find" are the same data. The generator correctly reasoned it was providing position data for the drawing agent and put coordinate values in the whitelist label list (satisfying the general answer-leak rules from DEF-026/028). Nothing in those rules recognised this collision. CLASS: answer-leak in diagrams (same class as DEF-026, DEF-028 — both previously "resolved" — confirming prompt-only fixes for this class can regress). FIX (27 May 2026): (1) `prompts/question_gen.txt` — new COORDINATE GRID QUESTIONS — SPECIAL RULE block: for questions asking the child to read coordinates, the whitelist label list must contain the letter name only ("Point P") — never the coordinate values. Position is described in the prose paragraph (so the drawing agent knows where to place the dot) but is excluded from the label list. GOOD and BAD `diagramPrompt` examples added. (2) `prompts/diagram_system.txt` — "Graphs" rule extended with explicit exception: for coordinate grid diagrams where a labelled point is the thing the student must identify, do NOT add coordinate values next to the point — letter name only. (3) `prompts/audit.txt` — criterion 3 extended with explicit coordinate check: a `diagramPrompt` that includes coordinate values next to a named point (e.g. "label point B as 'B (3,-2)'") fails the audit for coordinate-identification questions. NOTE ON ROBUSTNESS: this fix is three probabilistic LLM layers (generator, drawing agent, audit agent) with no deterministic backstop. The audit checks `diagramPrompt` text, not the rendered SVG — a drawing agent that ignores the whitelist still passes. A deterministic SVG text scan for coordinate patterns (`/\(\s*-?\d+\s*,\s*-?\d+\s*\)/`) after `processDiagrams` in `index.js` — analogous to the `compute.js` gate for DEF-041 — is the recommended hardening (not yet implemented). Fix was in `main` before the question-generation-stuck branch was closed; Tester test added 12 Jun 2026. RELATED: DEF-026, DEF-028, DEF-044. | CRITICAL | RESOLVED (12 Jun 2026) — Tester: prompts.test.js (5 assertions: COORDINATE GRID rule section present, letter-name-ONLY requirement, GOOD example, BAD example, diagram_system.txt exception) | Designer |
| DEF-044 | Empty pipeline marks job `complete` → frontend polls forever. When the pipeline produced 0 surviving questions, `index.js` wrote `status:"complete"` with `questions:[]` and returned. The `job-status` poller then entered a limbo state — all five frontend polling paths (`callEdgeFunction`, `resumePendingJob`, `recheckJob`, `replaceFlagged`, `checkPendingJob`) gate on `status === 'complete' && questions?.length > 0`, so an empty-complete job matched neither the complete nor the failed branch and polled for approximately 6 minutes before timing out. Surfaced 27 May 2026 by the replacement attempt for the DEF-043 questions (job `59f341a6-ecd4-4693-93d0-84bafe0095d4`): regenerated coordinate questions were correctly rejected by audit, the top-up produced nothing, `final.length === 0` fell through to the success branch. Stuck live row manually repaired via SQL (`UPDATE generation_jobs SET status = 'failed', error = '...' WHERE id = '59f341a6-ecd4-4693-93d0-84bafe0095d4'`). ROOT CAUSE: `index.js` had no zero-question terminal branch; only explicitly thrown errors reached the `failed` path. The `final.length === 0` case was a silent fall-through. FIX (27 May 2026): guard added in `index.js` immediately before the bank-write block — if `final.length === 0`, calls `updateJob` with `status:"failed"` and an explanatory error message, returns early. All five frontend polling paths already handle `"failed"` as a terminal state with appropriate user-facing messages (error text, bank fallback, or retry button depending on the call site) — no frontend change required. Fix was in `main` before the question-generation-stuck branch was closed; Tester test added 12 Jun 2026. RELATED: DEF-043, DEF-004. | MAJOR | RESOLVED (12 Jun 2026) — Tester: prompts.test.js (2 assertions: final.length === 0 guard present; zero-question path sets status:'failed') | Orchestrator |
| DEF-041 | Wrong correct-answer + wrong worked explanation in a generated question. "Daily customers… range?" data {45,38,52,41,64,78,56}; true range = 78−38 = 40. App marked correct answer as 33 with explanation "78−45=33" — used Monday's 45 as the minimum instead of Tuesday's 38. Child answered 40 (correct) and was marked wrong. ROOT CAUSE: the old Stage 2 `verify()` was not a missing check but an actively wrong one — it regex-matched the last `=number` in the explanation, treated it as truth, and overwrote `c` to match. Generator's answer and explanation shared the same slip, so making them agree cemented the error; audit/child agents never recompute arithmetic. CLASS of problem: numerical errors in any computed-answer question (range, mean, median, mode, totals, %). FIX (25 May 2026): `verify()` deleted; replaced by a deterministic Compute Engine in `compute.js` (`computeVerify`). Engine recomputes the answer from a structured `compute:{op,inputs}` block — never reads `c` or `e` — then confirms `c`, corrects `c` to the verified value, or rejects the question (fail-closed: unknown op / missing block / tied mode / answer-not-in-options all reject). Generator now emits `verify` + `compute` fields (`prompts/question_gen.txt`); `prompts/audit.txt` criteria 4 & 7 narrowed so the auditor never re-judges or overrides a compute-verified answer; `index.js` `audit()` hard-guards the same in code. Top-up questions also pass through the engine. TESTER: `compute.test.js`, 14 cases incl. the exact DEF-041 question (min not first in list) and the audit-rewrite guard — all pass. Live deploy confirmed: GCP logs show the engine correctly rejecting an unverifiable `mean` question. | CRITICAL | RESOLVED (25 May 2026) — Tester: compute.test.js | Reviewer |
| DEF-053 | "Run sweep" 404s: `Claude 404: ... "model":"claude-sonnet-4-20250514"` (reported 21 Jun 2026, screenshot). Claude Sonnet 4 (`claude-sonnet-4-20250514`) retired 15 Jun 2026 and now 404s. ROOT CAUSE: the `run-sweep` Edge Function (`supabase/functions/run-sweep/index.ts`) took its model from `Deno.env.get("CLAUDE_MODEL")` with a **hardcoded fallback** to the retired id, and — unlike the worker (`index.js` `getConfig`) — **never read the `runtime_config` table**. The function's `CLAUDE_MODEL` secret was unset, so it fell back to the retired literal. The admin had set `runtime_config.CLAUDE_MODEL = claude-opus-4-8` on the config page (10 Jun 2026), but that value never reached the sweep — the config page did not actually govern the sweep model. CLASS: the same retired model id was hardcoded as a fallback in five files (`index.js`, `scripts/curriculum_sweep.js`, `scripts/depth_tag_check.js`, `migrations/0001_admin_runtime_config.sql`, the edge function); any retired id silently 404s, and the sweep diverged from the admin-configured model. FIX (21 Jun 2026) — bring the model fully out of code into config: (1) Edge Function: removed the hardcoded model; added `resolveModel()` reading `runtime_config?key=eq.CLAUDE_MODEL` via the existing service-role headers and **failing closed** (500 "CLAUDE_MODEL not set in runtime_config — set it on the admin config page") when absent — no buried fallback. (2) CLI scripts read the same key via `resolveModel()` (config is source of truth; `CLAUDE_MODEL` env is an offline fallback; throws if neither yields a model) — no model literal. (3) `index.js` keeps its single env-overridable `CONFIG_DEFAULTS` default, value updated to the live `claude-sonnet-4-6`. (4) migration seed (config-layer default for a fresh DB) updated to `claude-sonnet-4-6`. No frontend change — the config page already lists/edits `CLAUDE_MODEL`. Edge Function redeployed via Supabase MCP. | MAJOR | RESOLVED (21 Jun 2026) — Tester: sweep.test.js (DEF-053 block: retired id absent repo-wide; sweep paths carry no model literal; edge fn + CLIs read runtime_config CLAUDE_MODEL; edge fn fails closed when unset; migration seeds a live default) | Orchestrator |
| DEF-050 | In-app "Run sweep" panel (subject + year + topic selection in the admin Curriculum tab) vanished from the live previews. Reported 10 Jun 2026: "Sweep is broken. Topic and year selection was available but not anymore" — raw.githack and GitHub Pages both serve `main`, and `main` never received the feature. ROOT CAUSE: commits `b751b0b` (the panel + `supabase/functions/run-sweep/index.ts` + taxonomy move to `frontend/`) and `0da3fbd` (confirmed working) were pushed to `claude/optimistic-allen-dsblK` AFTER PR #17 from that branch had already been merged — the merge captured only up to `8d06e64`, stranding the two later commits on the branch. The failure was silent because the feature's server half (the `run-sweep` Edge Function, deployed via MCP) stayed live in Supabase while its UI half never reached `main` — nothing in the repo or tests asserted the frontend wiring existed. CLASS: work split across git (frontend) and MCP-deployed state (Edge Functions, migrations) can half-ship; and pushing to a branch after its PR merges strands commits invisibly. FIX (10 Jun 2026): merged the stranded commits from `origin/claude/optimistic-allen-dsblK` (clean, no conflicts) restoring the panel, the function source, and the canonical `frontend/curriculum_taxonomy.json`. GUARD: `sweep.test.js` structural assertions that `frontend/admin.html` contains the Run sweep wiring (subject/year/topic controls + `run-sweep` call) and that the taxonomy JSON lives at the single path both app and CLI read. PROCESS RULE: after a PR merges, verify `git log main..branch` is empty before ending the session. | MAJOR | RESOLVED (10 Jun 2026) — Tester: sweep.test.js (admin wiring guard) | Orchestrator |
| DEF-049 | CR-022 was logged ✅ DONE having shipped only 1 of its 6 specified pillars. The shipped slice is 1D width only: each question carries a free-text `subtopic` label, steered across approved sub-strands, shown as a chip strip. MISSING: (a) the depth axis — `depth_bands` are stored in `curriculum_objects.payload` but no question is tagged with a depth band, so the spec's 2D `sub_strand × depth` grid does not exist; (b) the per-child coverage matrix; (c) the width-adaptive driver ("drive, not just measure"); (d) the parent mastery recommendation; (e) width/depth-aware bank top-up. CLASS: a DONE label that overstates delivery hides the remaining work and corrupts the backlog (prove-don't-assume applies to the log itself). Sub-risk found while here: a legacy banked/answered row with no `depth` must count as depth-UNKNOWN, never as "all bands covered", or a 100%-recall tutorial would falsely read as mastered. FIX: the CR-022 continuation (see CR_LOG.md) — slices A–E. Closed only when the two spec-mandated Tester tests pass and the depth-tag agreement number is recorded. RELATED: DEF-036 (topic tagging), CR-027 (few-shot anchoring informs the depth-anchor approach). | MAJOR | CODE COMPLETE (9 Jun 2026) — the 5 sub-slices are built + tested (`coverage.test.js`, incl. the two spec-mandated tests); the two unbuilt-pillar fixes landed. **DEPTH-TAG AGREEMENT RECORDED (14 Jun 2026): 95.0% (38/40).** Measured over the 40 depth-tagged questions banked since the CR-022 worker went live (4 completed jobs: Graphs & Functions ×3 + Expressions & Equations), judged independently against the recall/procedure/application/reasoning rubric (the `depth_tag_check.js` method; run inline because the container has no `ANTHROPIC_API_KEY`). Both disagreements were single-band adjacent slips on single-step substitution/transformation items (application→procedure ×1, recall→procedure ×1) — no wide swings (no recall↔reasoning confusion). 95% clears the conventional ~80% inter-rater bar. REMAINING for final close: owner decision to promote the parent recommendation observed→confident (flips `renderCoverageMastery` wording + `recommendation({confident:true})`) — held for owner sign-off because it is a parent-facing mastery claim. NB the agreement check was itself blocked until DEF-051 (bank writes silently failing) was fixed the same day. | Curriculum |
| DEF-045 | Diagram draw prompts describe a line by a single endpoint pair and hope, producing a diagonal instead of the real construction. Live evidence (5 Jun 2026): Q "A quarter circle has a radius of 8 cm. What is its area, to 1 decimal place?" → generated diagramPrompt "Draw a quarter circle in the top-right corner of a square … Show the radius line from the corner to the curved edge." Claude drew a single DIAGONAL labelled 8 cm (corner→curve), not the radius; the sector/centre geometry was also wrong. ROOT CAUSE: the instruction is wrong — singular "radius line" described corner-to-curve reads as a diagonal. A quarter circle (sector) is TWO radii along the two straight edges meeting at the centre at a right angle, with an arc joining their far ends. CLASS of problem: every geometry topic that describes a line by its endpoints (Angles & Lines, Shapes & Area, Circles, Pythagoras & Trig, 3D Shapes) can hit the same fault. This is NOT the same as the deleted CR-021 vision reviewer — that was symptom-patching Stage 3's blindness; this fixes the source. FIX (5 Jun 2026): (1) `prompts/question_gen.txt` — new CRITICAL rule #10 (GEOMETRY LINES: describe every line by ROLE + the two points it joins; name each of a shape's same-type lines separately; mark right angles), a correct quarter-circle GOOD diagramPrompt example (two radii, arc, right-angle square), and the failing prompt recorded as a BAD example. (2) `prompts/diagram_system.txt` — GEOMETRY rule + a complete WORKED SVG EXAMPLE (quarter circle: two perpendicular radii, arc, right-angle marker, one label per radius) so the drawing agent learns by example ("show, don't describe"). TESTER: `prompts.test.js` (15 structural assertions — geometry rule present, quarter-circle example shape, worked SVG is a sector with an arc command + right-angle polyline + two radius labels + 800x600 viewBox), wired into `npm test`. Manual: worked SVG eyeballed (correct sector, not a diagonal). | MAJOR | RESOLVED (5 Jun 2026) — Tester: prompts.test.js | Designer |
| DEF-052 | Angle-diagram quality regression. Owner reported "diagram generation has deteriorated" (14 Jun 2026) with two live "Set a Tutorial" screenshots, both on the **Angles on a straight line** sub-strand. Q1 "Two angles on a straight line are 117° and x°": the ray inclination matched neither value, the given `117°` arc looked acute while the `x` (=63°) arc looped larger — relative sizes inverted. Q4 "One angle is 65° and the other is x°": the `65°` arc sat detached far from the vertex, a second arc hugged the vertex, and the ray was ~45° (matching neither 65° nor 115°). A child could not tell which arc was which. ROOT CAUSE: the **DEF-045 class generalised to angles**. Both prompts teach geometry by worked example, but the examples only covered sectors/quarter-circles and right-angled triangles — there was NO rule and NO worked example for the most common case: two rays meeting at a vertex with an arc marking the angle. The drawing agent was left to invent the degree→geometry mapping, so it drew detached arcs, mismatched arc sizes, and arbitrary ray angles. The generator's only angle example (bookshelf/72°) described a scenario but never required the ray be drawn at the known angle's true inclination, the arc be centred on the vertex, or the unknown be marked `x` (not computed/leaked). CLASS: every angle topic (angles on a straight line, around a point, vertically opposite, in a triangle). FIX (14 Jun 2026, Designer): (1) `prompts/diagram_system.txt` — new ANGLES rule (arc centred EXACTLY on the vertex, small ~40-55px radius; rays drawn to TRUE inclination via cos/sin so the drawn opening matches its label; each shared-vertex angle its own distinct-colour arc of comparable radius; label on the bisector just outside the arc; KNOWN angle labelled with its value, UNKNOWN labelled exactly as given e.g. "x", never compute/reveal it) + a complete WORKED SVG EXAMPLE (angles on a straight line: horizontal line through a vertex dot, one ray at a true 130° inclination, the obtuse arc labelled "130°" and the acute arc labelled "x", both arcs centred on the vertex; coordinates derived with trig — both arc centres verified to land exactly on the vertex via the W3C endpoint-arc algorithm; rendered and eyeballed). (2) `prompts/question_gen.txt` — rule #10 (GEOMETRY LINES) extended with an ANGLES clause + a GOOD "angles on a straight line" diagramPrompt example (the exact live failing topic; unknown shown as `x`, value never stated → answer-leak guard, DEF-026/028 class). TESTER: `prompts.test.js` block 13 — 15 structural assertions (gen ANGLES clause + GOOD example + unknown-as-x; draw ANGLES rule + true-inclination + worked SVG with ≥2 arc commands, vertex dot, 800×600 viewBox, "130°" label, and the no-leak guard that the SVG shows "x" and never the computed 50°). Wired into `npm test` (full suite green). NB no `index.js`/`compute.js`/`review.js` change — `drawDiagram` passes the system prompt through unchanged; fix is entirely in prompt text. Live end-to-end generation not runnable here (no `ANTHROPIC_API_KEY` in container); a real Angles-on-a-straight-line tutorial should be eyeballed post-deploy before final close, as for DEF-040. | MAJOR | FIXED (14 Jun 2026) — awaiting visual QA — Tester: prompts.test.js (block 13, 15 assertions) | Designer |
| DEF-054 | Wrong correct-answer marked on a money "which coins make Xp" question; child's correct answer marked wrong. Live screenshot (26 Jun 2026), Year-2 money tutorial. Q7 "Five 2p coins make 10p. Which other coins make 10p?" — child answered "5p + 5p" (= 10p, CORRECT) and was marked WRONG; the app's stated correct answer was "10p + 10p" (= 20p, not 10p). The explanation read "5 + 5 = 10p, the same amount made a different way" — i.e. it justified the child's option while `c` pointed at a different, wrong one (mis-indexed `c` with a self-consistent explanation pointing elsewhere). Same batch, Q6 "Which set of coins also makes 50p?" marked "Only one 50p coin can make 50p" (false) while its own explanation gave "20 + 20 + 10 = 50p" — same class. ROOT CAUSE: coin/money "which coins make/total/also-make Xp" questions are deterministically calculable, but each *option is a sum of several coins*, not a single number — `compute.js optionValue` reads only the first number in an option, so the existing `equal_to` op cannot be used. The generator therefore tagged them `verify:"none"`, which SKIPS the deterministic Compute Engine entirely (`index.js` generate-filter) and leaves answer correctness to the probabilistic audit/child agents (`review.txt` criterion 4), which never recompute arithmetic — so the mis-indexed `c` reached the child. CLASS of problem: the DEF-041 / DEF-042 class (computed-answer & select-among-options MCQs bypassing the engine) extended to money-combination questions whose options are multi-coin sums. FIX (26 Jun 2026): new deterministic op `coin_match` in `compute.js` — `compute:{op:"coin_match","target":<pence>}` with NO inputs array; the engine parses the money tokens of EACH displayed option (`optionPenceTotal`: `£X`→×100p, `Np`→Np, summed), finds the single option whose total equals `target`, and confirms / corrects / rejects `c` — verifying the option text the child actually sees, never the model's `c`/`e`. Fail-closed: an option with no parseable money token, zero matches, or ≥2 matches → reject (dropped + topped up), which also blocks the Q6 prose-option trap. `index.js` unchanged — the generate-filter already routes `verify:"arithmetic"`+`compute` to the engine, and `coin_match` sets `_computeVerified`/`_computeCorrected` so `review.js` surfaces `computeVerified:true` and the auditor (criteria 4 & 7) does not re-judge it. Paired prompt change (`prompts/question_gen.txt`): `coin_match` op doc + a GOOD worked example (the corrected Q7) and a BAD example (the exact live failure) per "teach with examples". | CRITICAL | RESOLVED (26 Jun 2026) — Tester: compute.test.js (DEF-054 block: money-parser unit cases incl. mixed £/p; exact Q7 c-correction; happy path; two-match + no-match + no-token + missing-target rejects) | Reviewer |
| DEF-051 | Every bank write silently failed → `question_bank` permanently empty. The worker's `bankWrite` (`index.js` Stage 6) builds rows from the flat columns (`q`, `o`, `c`, `e`, `svg`, `depth`, `sub_strand`, …) and never sets the legacy `question` jsonb column, which was `NOT NULL` with no default. Every POST to `/rest/v1/question_bank` therefore returned HTTP 400 (NOT NULL violation). `fetch()` does not throw on HTTP error status — only on network failure — and `bankWrite` wrapped the call in `try/catch` with no response-status check, so the 400 was swallowed with zero log output. CONSEQUENCES: (a) the bank never accumulated a single generated question, so `adaptBank` (Stage 0) had nothing to serve — every tutorial paid full generation cost and the CR-022 width/depth bank top-up tiers were dead; (b) no `depth`-tagged rows ever landed, which is why the CR-022 depth-tag agreement check (DEF-049) found an empty bank across three deploy attempts. CLASS: `fetch()` HTTP errors swallowed by a status-blind `try/catch` — any other write helper with the same shape (`record`, `updateJob`) is the same latent risk. Discovered 14 Jun 2026 via Supabase API logs (`POST | 400 | /rest/v1/question_bank | node`) after adding response-status logging to `bankWrite`. FIX (14 Jun 2026): (1) migration `0005_question_bank_nullable_question.sql` — `ALTER TABLE question_bank ALTER COLUMN question DROP NOT NULL` (the column is dead legacy; nothing reads or writes it). (2) `bankWrite` now checks `res.ok` and logs `HTTP <status> — <body>` on failure, so a future bank-write rejection is loud, not silent. Migration applied to `rtyvomkhajyinlycgjzm`; verified the column is now nullable. | CRITICAL | RESOLVED (14 Jun 2026) — Tester: needs a structural assertion that bankWrite checks res.ok (follow-up) | Orchestrator |
