{
  "name": "brightmind-worker",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "npx functions-framework --target=worker --port=8080",
    "test": "node compute.test.js && node review.test.js && node prompts.test.js && node config.test.js"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0"
  }
}
